---
- name: Install global npm packages
  npm:
    name: "{{ item.name }}"
    global: true
    state: present
  loop: "{{ npm_global_packages }}"
  loop_control:
    label: "{{ item.name }}"
  ignore_errors: true
