---
- name: Configure Claude Code Puppeteer MCP
  shell: claude mcp add puppeteer
  register: mcp_result
  failed_when: false
  changed_when: "'added' in mcp_result.stdout | default('')"

- name: Configure GitHub CLI auth reminder
  debug:
    msg: "Remember to run 'gh auth login' to authenticate with GitHub"
